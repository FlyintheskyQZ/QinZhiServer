<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="seu.qz.qzserver.mybatismapper.AppCustomerMapper">

    <select id="getCustomerById" resultType="seu.qz.qzserver.entity.AppCustomer">
        select * from appcustomers where user_id = #{id}
    </select>

    <select id="getCustomerByName" resultType="seu.qz.qzserver.entity.AppCustomer">
        select * from appcustomers where user_nickName = #{name}
    </select>

    <select id="getCustomerByRegisterName" resultType="seu.qz.qzserver.entity.AppCustomer">
        select * from appcustomers where user_registerName = #{user_registerName}
    </select>

    <select id="getCustomerByPhoneNumber" resultType="seu.qz.qzserver.entity.AppCustomer">
        select * from appcustomers where phoneNumber = #{phoneNumber}
    </select>

    <select id="getAppCustomersByPhoneNumber" resultType="seu.qz.qzserver.entity.AppCustomer">
        select * from appcustomers where phoneNumber = #{phoneNumber}
    </select>

    <select id="getCustomerByEmail" resultType="seu.qz.qzserver.entity.AppCustomer">
        select * from appcustomers where email = #{email}
    </select>


    <select id="getCustomersByEmail" resultType="seu.qz.qzserver.entity.AppCustomer">
        select * from appcustomers where email = #{email}
    </select>

    <insert id="addCustomer" parameterType="seu.qz.qzserver.entity.AppCustomer">
        insert into appcustomers(user_nickName, user_password, user_balance, user_registerName, user_identityId, register_time,
         authority_level, related_device_id, phoneNumber, email, numberForProvideOrders, numberForFinishedOrders, isMale)
        values(#{user_nickName}, #{user_password}, #{user_balance}, #{user_registerName}, #{user_identityId}, #{register_time},
         #{authority_level}, #{related_device_id}, #{phoneNumber}, #{email}, #{numberForProvideOrders}, #{numberForFinishedOrders},
          #{isMale})
    </insert>

    <delete id="deleteCustomerById">
        delete  from appcustomers where user_id = #{id}
    </delete>

    <update id="updateCustomer">
        update appcustomers
        set user_nickName = #{user_nickName}, user_password = #{user_password}, user_balance = #{user_balance}, user_registerName = #{user_registerName},
        user_identityId = #{register_time}, authority_level = #{authority_level}, related_device_id = #{related_device_id}, phoneNumber = #{phoneNumber},
         email = #{email}, numberForProvideOrders = #{numberForProvideOrders}, numberForFinishedOrders = #{numberForFinishedOrders},
          isMale = #{isMale} where user_id = #{user_id}
    </update>

</mapper>